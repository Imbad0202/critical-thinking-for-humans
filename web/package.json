{
  "name": "critical-thinking-for-humans-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "cd .. && git ls-files -z -- '*.js' '*.mjs' | xargs -0 -n1 node --check",
    "check:i18n": "node scripts/check-i18n.mjs",
    "test:daily": "node --test tests/daily-*.test.mjs",
    "daily:sync-public": "node scripts/sync-daily-public.mjs",
    "daily:upload": "node scripts/upload-daily.mjs"
  },
  "dependencies": {
    "@vercel/blob": "2.6.1"
  }
}
